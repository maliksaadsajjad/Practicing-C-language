#include<stdio.h>
#include<conio.h>
main()
{
 int length;  // Declare a variable to store the length of the rectangle.
 int width;   // Declare a variable to store the width of the rectangle.
 int area;    // Declare a variable to store the area of the rectangle.
 printf("Enter the length of the rectangle: \n");
 scanf("%d",&length);
 printf("Enter the width of the rectangle: \n");
 scanf("%d",&width);
 area = length*width;
 printf("The area of the rectangle is : %d ", area);
 getch();
}
